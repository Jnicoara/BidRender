# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.dist

# Generated files
*.tsbuildinfo
coverage/

# Drizzle's migration snapshots.
#
# Written by `drizzle-kit generate`, read by drizzle-kit to work out what
# changed since the last migration. Nobody reads them by hand and nobody edits
# them, so formatting them buys nothing — and it costs: reformatting seventeen
# generated files is churn on every `pnpm format`, and the next `db:push` writes
# them back in drizzle-kit's own style anyway, so the diff never settles.
drizzle/meta/

# Captured test data — real text items from public bid sets, written by a
# script and compared by value. Pretty-printing 78KB of one-line JSON into
# thousands of lines is churn with nothing to read.
**/__fixtures__/*.json

# Package files
package-lock.json
pnpm-lock.yaml

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log

# Environment files
.env*

# IDE files
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db
