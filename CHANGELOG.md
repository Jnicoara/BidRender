# Changelog

Plain-English record of what changed and when. Newest first.

This is the human-readable companion to the git history — read this to see what happened, read the commits for the technical detail.

## [2026-09-14]

- **HelixBid is now BidRender.** The old name clashed with an existing company. Everything a user sees now says BidRender: the sign-in screen, the landing page, browser tabs and link previews, the installed app's name and icon (the yellow "HB" mark is now "BR"), messages across the app, and the invoice numbers in the QuickBooks export (BR-501 instead of HB-501). A phone that already has the app on its home screen may keep the old name and icon until it is removed and added again — an iPhone always will.
- **A brand-new database can now be built from the app's own update files.** New hosting needs exactly that, and it stopped after 4 of 44 steps without showing any error: one step used a name a single character longer than MySQL allows, and a later one used wording the Manus database accepts but standard MySQL does not. Both are fixed. A failed update now says which file, which step and MySQL's reason, and a check stops either mistake from coming back. Existing databases are unaffected.
- **Removing a plan now says exactly what it will destroy.** The warning used to say only the sheet names and scales would go, and that you could attach the file again. In fact removing a plan permanently deletes every stamp, traced run, circuit and plan-reader result on its sheets, and attaching the file again brings none of that back. The warning now counts what is on the plan — for example 14 stamps and 3 traced runs — says it cannot be undone, and keeps the delete button disabled until that count has loaded. The trash icon that opens it is also always visible and big enough for a fingertip, instead of appearing only when a mouse hovers over the row.
- The Takeoff spec now records the first decisions — fix the plan-removal warning first, keep location tags, send counts to the bid live — and adds sheet coverage, schedule cross-checks, makeup allowances and a routing waste factor as requirements, with vertical rise, revision comparison and bid-to-plan links written up as proposals.
- **Wrote down everything the Takeoff screen is supposed to do, and how much of it really works.** The rebuilt screen was planned from a document that never mentioned zoom, pan, removing a mark or getting counts onto the bid, so those were dropped without anyone noticing. The new master spec, `references/takeoff-spec.md`, compares the old screen, this changelog and the current code feature by feature, marks each one as working, half-built or missing, lists every sentence on the screen that overstates what it does — including six earlier entries in this changelog — and lays out the decisions still to make. It is now what the Takeoff screen gets checked against.
- **Plans now draw.** Opening any real plan on the Takeoff screen failed with "Cannot read properties of undefined (reading 'createElement')". The part of the app that draws pages in the background was reaching for pieces of the web page it cannot see from there — to load a drawing's fonts and to make scratch canvases — so a blank test page drew and every real sheet did not. This was broken everywhere the app runs, not only on a local copy; nobody saw it because plan uploads were failing first. Fonts are now drawn as outlines and scratch canvases are made the background-safe way, and the sheet looks the same.
- **HelixBid can run on a laptop with no Manus services.** Three new settings, all off unless set: plans and logos can be kept in a folder on the computer instead of cloud storage; the AI features can be switched off, with the plan reader's panel hidden rather than showing failures; and the nightly backup and archive purge can be left switched off. The live site sets none of them, so it behaves exactly as before.
- **Email-and-password sign-in works on a plain http address.** Browsers were silently throwing the login cookie away there, so signing in on a local copy bounced straight back to the form. Secure https sites, including the live one, are unchanged.
- **The Manus page-editor script and Manus debug logger no longer load.** The editor script was being added to every page, the live site included; the logger copied browser activity, sign-in tokens included, into a log folder during development. Neither does anything off the Manus platform. An unused owner-notification action that went through Manus is gone too.
- **The app no longer crashes when no Manus login address is set** — it shows its own sign-in page instead.

## [2026-08-19]

- **A backup that cannot read a file now says so without crying wolf.** The nightly backup treated any unreadable stored file as a total failure. With a storage fault making some plan PDFs unreadable, that meant a failure alert every single night for something nobody could act on — and an alert that fires nightly is one people stop reading, which is how a real failure gets missed. A run now ends in one of three states: everything copied, **partial** (the database is safely backed up but some stored files could not be read), or genuinely failed. Only a real failure is retried, because a storage refusal gives the same answer on the second and third try and each retry costs a full database dump. Partial is never reported as success — it names every file it could not read, in the log and in the record stored alongside the backup itself.

- **A plain-English screen when something breaks, instead of a wall of code.** If a screen ever fails to load, the app used to show a page of programmer error text — useless to read, and alarming enough to look like the app had lost your work. It now says what happened in one sentence, says plainly that nothing you had saved was affected, and gives you a button back to the Dashboard and one to try again. The technical detail is still kept, just not shown: it is saved in the browser so it is not lost, and a small "Copy the details for support" link puts the whole thing on your clipboard to paste into a message when you report a problem.

- **The plan screen stopped underselling itself.** Its subtitle still read "Pick a sheet, set its scale. Measuring and counting come next." — written when that was true, and left there after stamping, conduit and cable tracing, the symbol legend, layer visibility and the AI plan reader all shipped. Anyone reading the top of the screen was being told the main thing they came for did not exist yet. It now says what the screen actually does.

- **Retired the Feature Flags panel in Admin, which had quietly stopped doing anything.** It listed toggles and stated on the page that switching one took effect immediately for contractors. Nothing had read those toggles for some time — what features an account can see is decided by its access tier now — so every switch on that page did nothing at all while claiming otherwise. A control that lies about working is worse than no control, so the panel is gone rather than reworded. The rest of the Admin screen — early access signups, the platform role reference — is unchanged and now says plainly that there is no control for unreleased features there yet.

- **Mistyped web addresses land somewhere useful instead of on a broken-looking 404.** Because the app puts its addresses after a "#", typing something like helixbid.app/settings by hand — or following a link written before that changed — hit a leftover error card from the original project template: a white-and-blue "Page Not Found" box inside an otherwise dark app, saying the page may have been deleted when it had not. Those addresses now open the screen they name, unrecognised ones open the Dashboard, and either way the address bar tidies itself up so the bookmark works properly next time.

## [2026-08-18]

- **A tidy-up across the screens you use most.** The bid header had grown to five buttons; the three that are really "send this bid to someone" — proposal, materials list, accounting export — are now one **Send** menu that names who each one is for, leaving Plans and Count out where they belong. The panels down the right-hand side of a bid (charges, scope, pricing settings) start closed with a line saying what is in them — "3 charges · $420", "Following your company defaults" — and remember how you left them. On the plan takeoff screen the layer filters fold away too, though the "some marks are hidden" warning deliberately does not. The Dashboard now graduates: once you have finished the getting-started checklist the two big start cards fold into a single **New bid** menu carrying the same three routes, so the board starts higher up the screen. And the All/Mine filter on the library screens stops appearing before you own anything to filter to.

- **Includes and excludes are laid out the way the proposal prints them.** They used to be one flat list in the order you typed them, every line tagged "Incl" or "Excl" so you could tell which was which — while the proposal itself has always printed them as two columns, Included and Not included. Now the editor does the same, so you arrange the scope in the shape your client will read it in. It also takes fewer controls: there is no kind dropdown any more, because the column you type in is the kind, and no per-line tag, because the column says it. The whole block sits closed with a count on it ("2 included · 3 excluded") until you need it.

- **Settings is six panels instead of one long scroll.** Pricing, Branding, Sales tax, Proposal, Display and Account, one at a time. The thing that made this worth doing is not the length: your overhead, profit and productivity defaults carry a warning that changing them moves every new bid _and_ every existing bid still following the default — and that warning was sitting three sections down a page most people never scrolled to the bottom of. Pricing is now what you land on. Each panel has its own address, so the Proposal screen's "Open branding settings" and "Open tax settings" buttons finally go to branding and tax rather than to the top of everything. Two smaller things went with it: the greyed-out "Units — metric coming soon" block, which nobody could press and everybody had to read past, and the row of seven text-size buttons that offered exactly the same seven sizes as the slider directly beneath them.

- **Quick bid starts counting in one click.** It used to open a page asking which bid you meant, listing every bid you had ever written — before you could count anything. Now the Quick bid card on the Dashboard makes the bid and drops you straight into counting with the cursor already in the search box, exactly the way "Upload a plan" beside it already worked. The new bid is named for the day so it is easy to find again, and the name is editable the moment the screen opens. A bid you already have gets a **Count** button in its header next to Plans — the two ways of putting quantities on a job, off a drawing or off a count in your head, now sit side by side.

- **The sidebar went from fourteen items to eight.** Three of the fourteen were not really screens: "Bids" opened a list that the Dashboard already showed in full, "Quick bid" opened a page whose only job was asking which bid you meant, and "Supplier Pricing" showed the same materials as the catalog with different columns. Kits and Modifiers are now tabs on Assemblies, because a kit is a group of assemblies and a modifier adjusts one — neither means anything on its own. Supplier pricing is a tab on Materials, so there is no longer a right and a wrong screen to edit a material on. Crew moved down beside Settings, and the Archive left the sidebar but still appears on the Dashboard whenever there is something in it. Nothing was removed — every screen is still there, and every old link, bookmark or saved address still lands on the right place and quietly corrects itself.

- **Removed 10,000 lines of screens the app stopped using.** HelixBid was originally built around four estimating workspaces — Residential, Commercial, Civil, Industrial — each with its own projects and its own calculator. That design was replaced by the bid-and-library structure the app uses today, but the old screens were never deleted: an export builder, two earlier plan viewers, a chat box, and the project tabs belonging to the retired assembly system all still shipped to every user's browser, wired to nothing. They are gone, along with the shared state file that existed to feed them, which shrinks from about 1,200 lines to 40. Nothing on screen changes — the app does exactly what it did — but the download is smaller and there is a lot less code to mistake for the live app when working on it.

- **Crimp lugs sit together again.** A lug is sold by the span of wire it accepts — a 14-10, an 8-6 — and the library could only read three of those five as sizes, so the other two dropped to the bottom of the shelf away from the rest of the family. All five now list in one group, thinnest range first.
- **The materials library groups by type, so browsing a shelf is a short list again.** Wire & Cable was eighty-nine rows deep with six kinds of wire repeating at every gauge; it now reads as ten families — THHN, NM-B, MC cable — each with its sizes underneath. The same applies to conduit, its fittings, panels and breakers. Nothing was renamed, moved or added: the headings are drawn over the order the list was already in, and anything that does not divide into families (receptacles, switches, fasteners) stays exactly as it was rather than being forced into groups it does not have. Searching is untouched — typing "12 THHN" still lands straight on the wire.
- **The Dashboard stays quick however many bids you have.** It was drawing every live bid — at around a thousand that meant a thousand cards, forty screens of scrolling and a noticeable wait every time the app opened. Each column now shows the most recent twelve with a "show all" underneath, and the totals at the top of each column are still counted from every bid, so nothing about the money changed.
- **You can find a bid from the Dashboard.** The same search the Bids screen uses — by job name, client or address — now opens from the Dashboard too, so you no longer have to leave it to reach one job out of hundreds.
- **Enter adds a line on the bid screen.** Type part of an assembly name, arrow to the one you want, press Enter. Quick bid has always worked that way; the bid screen made you reach for the mouse for every line.

## [2026-08-16]

- **A close-out can be corrected.** Once you had recorded one, the per-assembly breakdown said "this bid has no assemblies" and refused to save — so fixing a single wrong figure meant deleting the whole record and typing every line again, and switching a job from one total to a per-assembly breakdown was impossible. Both now work, and reopening a breakdown shows the hours you entered.
- **Enter records the hours.** Typing the number and pressing Enter now saves it, instead of leaving you to reach for the mouse. On a per-assembly breakdown Enter moves down the column and the last one records, which takes a twenty-four assembly job from about two minutes to under thirty seconds.
- **Assemblies that ship with the app now come with a labour role attached**, so their hours are able to cost something as soon as you set your rate. Nothing is priced for you — the starter Journeyman rate still ships blank — but the blank is now on the Labor Rates screen, which tells you about it, instead of hidden inside a bid total.
- **A bid can no longer show hours with no money against them, silently.** Some assemblies ship without a labour role attached, and adding one put its hours in the total while pricing its labour at nothing — a bid could read "9.7 hours, $0.00" and look perfectly finished. The bid total now says how many lines are affected and what to do about it.

## [2026-08-15]

- **A new Performance screen shows how the business is actually doing.** Win rate over time, how much work is out for bid, and — from the hours entered at job close-out — whether finished jobs earned the margin they were quoted at, broken down by trade. Built entirely from bids and close-outs you already have; there is nothing to set up and no new field to fill in.
- **The win rate counts bids that were answered, not everything you quoted.** A bid still sitting on somebody's desk is not a loss, and counting it as one would make a busy month look like a bad one. The number of bids still out is shown right beside the rate everywhere it appears, so it can never be read out of context.
- **Profitability compares the hours a job took against the hours it was quoted at, priced at that bid's own labor rate.** Materials are held at their estimate, because the app records actual hours and not actual material cost — the screen says so at the top of the panel rather than presenting a partial figure as a full profit-and-loss.
- **Only owners and admins can see it.** Tighter than everything else in the bid layer: an estimator can open and price any bid, but company-wide win rate and margin are the owner's to share. Estimators and viewers do not see the menu item at all.
- **It stays fast on years of history.** Everything is totalled inside the database rather than by loading bids into the browser, so a contractor with a decade of quoting opens the screen as quickly as one with a month of it.

- **Tracked down the error on the archived-bids screen: nothing was wrong with the app, the database it was running against was out of date.** Opening the archive reads a bid's full record, which now includes the sales-tax fields and the sample marker added in recent updates. On an environment where those updates had not been applied to the database, that read failed outright and took the whole screen with it.
- **There is now a one-command way to ask whether a database is behind the code**, so the same thing is caught before a deploy instead of by someone hitting a screen — and a check that fails while building, if a change is ever made without its database update.

- **Fixed: anyone other than the account that owns the company could not attach a plan to a bid.** The stand-in upload route — the one every upload currently goes through, while the storage service is still waiting on a configuration change — looked the bid up under the person uploading rather than under the company. An admin or estimator attaching a plan to an ordinary company bid was told "Bid not found".
- **A read-only account can no longer upload a file through that route.** It sat outside the normal permission checks, so a viewer could push a file into the company's storage even though they cannot change a bid anywhere else in the app.

- **Closed a hole that let anyone who knew a file's address read it, signed in or not.** The address a plan sheet or company logo is served from used to hand over the file to whoever asked, with no check that they were logged in, let alone that the file was theirs. Knowing the address was the only thing protecting one contractor's plans from another's.
- **Plan and logo addresses now expire.** Each one is issued for a single file, signed so it cannot be edited to point at a different one, and stops working after about half an hour — at which point the app quietly issues a fresh one. Nothing changes in normal use: plans open and logos appear exactly as before.
- **A plan left open all afternoon keeps working.** If its address goes stale while you are still working in it, the viewer fetches a new one and carries on without saying anything. If the file genuinely cannot be read, you still get a plain error rather than a spinner that never stops.

## [2026-08-14]

- **A new account can open a finished example bid in one click.** The Dashboard now offers "Show me an example bid" — a small commercial retail buildout, 2,400 sq ft, priced end to end at around $16,800: 76 devices and fixtures, a labor modifier for the 14 ft ceiling, a permit and a lift charge, a client, and a proposal you can print. It answers "what does this thing actually produce?" before you have entered anything of your own.
- **It is a real bid, so every screen works on it.** Not a screenshot or a locked preview — you can change quantities, edit lines, see how the modifier and the markup move the total, and print the proposal. A preview that behaved differently from the product would teach the wrong thing.
- **It cannot be mistaken for one of your jobs.** It is marked SAMPLE on the dashboard, in the bid list, in search and across the top of the bid itself, and — the part that matters most — **it is left out of your "Out for bid" total**. A row on a list gets read; a headline figure does not, so a fictional amount inside it would be the worst version of this.
- **It cannot touch anything of yours.** Creating it writes one bid and one client and nothing else: your materials, assemblies, labor rates and company settings are not read or changed. Its prices live on the bid's own frozen line costs, so your catalog stays exactly as you left it. Delete it and your account is byte-identical to one that never made it.
- **It is offered, not imposed.** Nothing is created until you ask for it, the offer stops appearing once you have real bids of your own, and one click removes it along with its client.
- **Two things fixed while testing it on a genuinely empty account:** the sample was ticking "Complete your first bid" on the getting-started checklist — telling a brand-new user they had finished a bid when they had done nothing — and the "remove the sample" button could not be used without naming the bid.

- **HelixBid can be added to a phone's home screen and opens like an app** — full screen, no browser address bar, its own icon, its own dark splash screen. On Android and desktop Chrome the browser offers to install it; on an iPhone it is Share → Add to Home Screen.
- **It loads instantly on repeat visits, and opens even with no signal.** The app's own files are kept on the phone, so a truck with one bar starts up as fast as one on wifi. If there is genuinely no connection it still opens and tells you it cannot reach the server, instead of showing the browser's error page.
- **Your numbers are never cached.** Not one bid price, labor rate or tax figure. A stale price looks exactly like a fresh one on screen, and that is not a risk worth taking for a slightly faster load — so anything that comes from the server is always fetched from the server. This is deliberately not an offline-editing feature.
- **A new version is picked up straight away.** The most common complaint about installed web apps is that they get stuck on an old version; the app checks for the current one every time it opens, and clears out anything left from a previous release.
- **Installing is optional and changes nothing if you don't.** In a normal browser tab the app behaves exactly as it always has. Nothing requires installation and nothing is hidden behind it.
- The HB mark is now a proper app icon at every size phones ask for, including the full-bleed version Android needs so it is not shrunk into a circle with a dark ring around it.

- **You can close a job out and see how the estimate did.** On any bid there is now a Job close-out panel: enter the hours the job actually took — one total, or broken down per assembly — and it shows what you estimated, what it took, and the gap, as hours and as a percentage. It also tells you what productivity factor would have made that estimate right, as a reading rather than a button.
- **Closing out is optional and stays optional.** A bid you never close out prices, proposes, exports and archives exactly as it always did. The panel sits shut until you open it.
- **The estimate is frozen when you close out.** Editing an assembly afterwards will not quietly rewrite what an old job was estimated at — a comparison whose baseline moves is not a comparison, and this history is meant to be evidence.
- **If an assembly keeps running long, HelixBid says so — and does nothing about it.** After three or more closed-out jobs, if an assembly is consistently more than 10% off its estimate, a suggestion appears on your Assemblies library: "this has run 20% over across four jobs — change base hours from 2 to 2.4?" You accept it, dismiss it, or leave it. Nothing is ever applied for you, dismissing is remembered so you are not asked again, and a suggestion whose evidence stops holding up withdraws itself.
- **It refuses to mistake variance for a trend.** An assembly that ran well over on two jobs and well under on two others produces no suggestion — that is a mix of different jobs, not a wrong number, and an average alone would have reported it as one.
- **Recording hours needs the same permission as editing the bid. Accepting a suggestion needs permission to edit the library**, because that changes a number every future bid is built from. And your field data stays yours: two companies using the same starter assembly build entirely separate histories, and accepting a suggestion takes a private copy rather than moving the shared one.

- **You can search your bid history.** The Bids screen now has a search box across the job name, the client and the address, plus filters for trade, status, a date range (created or due), and whether to include archived bids. Built for finding a job from three years ago, not for scrolling.
- **The searching happens in the database, not in your browser.** Every filter and every page is worked out by the query, so the screen is as quick with twenty thousand bids as with twenty — and stays quick however deep you scroll. Tested against three thousand real bids, paged all the way through.
- **Searching by client finds bids that only have the client attached**, not just ones where the name was typed in. Those are different things in the data, and a search that only read one of them would quietly miss half your history.
- **The Clients screen got the same treatment.** It used to load every client and filter them in the browser — fine at twenty, not at four hundred. Search and paging are now the query's job, and the client picker on a bid searches as you type instead of downloading your whole customer list.
- **Fixed a paging bug found only by testing at volume:** a mismatch in how timestamps were read and written was quietly dropping one bid at every page boundary. At a handful of records it is invisible; at three thousand it lost twenty-nine.

- **You can put your crew in HelixBid.** A company can now have several people in it, each with their own login, all working on the same bids, library and pricing. There is a new **Crew** screen next to Clients: invite someone with a code, set what they can do, suspend them if they leave.
- **Four roles.** _Owner_ and _admin_ can do everything (an admin cannot act on the owner). _Estimator_ is the working role — builds bids, edits the library, manages clients, and can see costs, but cannot change your overhead, profit or labor rates, and cannot add people. _Viewer_ can only read. Your own account is the owner of your company and nothing about it changes.
- **Nothing about working alone changes.** If you never invite anyone, your account is a company of one and every bid, price and setting stays exactly where it was. The change needed no data moved.
- **Suspending someone takes effect on their next click**, not their next login — you do not have to wait for them to sign out.
- **Invitation codes are shown once and stored scrambled.** Nobody, including the app, can read a code back afterwards; if it is lost, issue another. Codes work once and expire after 14 days. There is no email sending yet, so you pass the code on however you normally reach your crew — that is stated plainly rather than hidden behind a Send button that does not send.
- **A second kind of account: internal.** For you and close testers, so unfinished features can go live for a few people before everyone. The QuickBooks export is the first thing behind it — the column format was worked out from documentation rather than by importing into a real QuickBooks company, and that is worth testing before it reaches anybody's books. Standard accounts do not see the button at all.
- **Fixed two access holes found while building this, before either shipped:** an estimator could change the company's overhead and profit (which re-prices every bid), and a suspended person was quietly given a fresh empty company instead of being turned away — so the suspension did nothing. Both were caught by tests written to attack the system rather than to use it.

- **A bid can now be sent to accounting as a QuickBooks CSV.** Customer, a reference, the date and the money — labor, materials, each additional charge, sales tax and the total. It imports through QuickBooks Online's own invoice importer (Settings → Import data → Invoices).
- **Your overhead, profit and cost figures do not go with it.** This needed more care than it sounds: the labor and material numbers on the bid are what you PAY, before overhead and profit, so exporting those two would have handed your margin on every job to anyone the file was forwarded to. What goes out is the split of what the customer is CHARGED, and the two lines add up to the bid price exactly. No percentage of any kind appears in the file.
- **The exported total always equals the bid's Total due**, and the export screen shows the lines and the total before writing anything, so it can be checked against the bid on screen.
- **Sales tax goes out as its own line at your figure, not as a tax code.** QuickBooks will not accept a tax amount on import — given a code it recalculates the tax from its own rate tables, which could quietly turn a bid your customer approved into an invoice for a different amount. The trade-off, worth knowing once: point the "Sales tax" product/service at your sales-tax liability account the first time you import.
- **It says what it cannot fix rather than staying quiet.** A bid with no customer name is flagged, because QuickBooks rejects those rows without saying so; a bid that has not been won is flagged too, since importing it creates an invoice for work you have not got.
- One bid at a time for now. Exporting several at once can follow if it turns out to be wanted.

- **Fixed a price import bug that quietly divided expensive prices.** A supply-house line reading `$1,250.00` was imported as **$250.00** — and reported as a success, so nothing on screen said anything had gone wrong. Any item over $1,000 was affected, which is exactly the expensive gear where being wrong costs the most. Prices now import at their real value.
- **Materials with a comma in their name can be priced now.** Thirty shipped materials — `#10 bare copper, stranded` and its relatives — could never be priced from a sheet, because the importer cut the name at the comma and reported it as unrecognised.
- **Pasting straight out of Excel works.** It used to import nothing at all and say "0 rows ready", because a spreadsheet paste is separated by tabs and the importer only understood commas. Tabs, semicolons and pipes are all read now, with or without a header row, and the importer says which it found.
- **The importer now shows what it read, before it writes anything.** The first few prices appear as it understood them, so a misread figure is caught while it can still be cancelled. Anything it could not read confidently is listed by line number with the reason, and is skipped rather than guessed at — an unreadable price used to vanish silently, leaving a material at its old cost while the user believed it had just been updated. A price that is genuinely ambiguous, like the European `1,25`, is refused rather than read a hundred times wrong.

- **A bid can now produce a materials list to send a supplier for a quote**, as a CSV or a PDF. It lists what to buy and how much of it — item, quantity, unit — and nothing else. No prices, no labor, no overhead, no markup, no bid total. What a contractor pays and what they charge stays inside the company; a supplier who could read the margin would be quoting against a hand held face up.
- **It works before the bid is priced, which is the point.** A contractor asks for a quote precisely because they do not yet know what things cost. The list is built purely from the take-off, so an untouched, unpriced bid gives exactly the same quantities as a finished one, and the button is there from the first mark on the drawing rather than at the end.
- **It is on both the Takeoff screen and the bid screen.** Stamping a plan and adding assemblies to a bid are separate things, and a bid can have either or both — a Quick Bid has no drawing at all — so both are read, and the button sits wherever the work is happening.
- **Identical materials from different assemblies combine into one line**, and the line says which assemblies it came from, so 120 wire nuts can be traced back to where they came from without opening anything.
- **Traced runs are reported as measured footage, kept separate from the counted items.** A traced path records a length but not what kind of pipe it is, so it is listed as footage awaiting a specification instead of being guessed at and quoted as the wrong material. A run on a sheet with no scale is called out in the notes rather than quietly left out, because a short list reads as a complete one to whoever receives it.

- **Each additional expense now has two tickboxes: Taxable and Marked up.** They are independent — a charge can be taxed but not marked up, marked up but not taxed, both, or neither — because which one is right depends on the state and on the kind of charge. A permit is often passed through at cost and untaxed; a minimum service call is usually treated like any other revenue.
- **Both start off, so nothing about existing bids changes.** Taxable puts the amount into the sales tax base alongside materials and labor. Marked up runs it through exactly the same overhead and profit as the rest of the bid, rather than a separate markup that could drift from it. A marked-up charge still shows as its own line at what the customer is actually charged, so it is never counted twice.
- **The tickboxes work the same on a saved expense and a one-off**, and can be changed on a charge already sitting on a bid. A saved expense carries its settings onto a bid when you add it, and changing them on that bid does not reach back and edit the saved one.

- **Bids can carry flat charges — permits, inspections, dispatch, a minimum service fee.** Keep the ones you use often on a saved list for quick re-adding, or type a one-off for a single job. A one-off stays on that job and is never added to your list — a $340 permit for one awkward site should not turn up as a suggestion on every bid afterwards. If it turns out to be worth keeping, there is a button to save it afterwards.
- **Charges are added to the price as they are.** Not marked up, and not taxed. They appear as their own named line on the bid and on the proposal, so a customer can see what a permit cost rather than finding it folded into a total.
- **Includes and excludes.** A section listing what the price covers and what it explicitly does not — standard bidding practice, and the thing that settles the argument three months later about who was supposed to pull the permit. Same pattern: save the ones you use on every job, or type a one-off. They print on the proposal, with the excludes given the emphasis.
- **A scope-only version of the proposal.** A toggle on the proposal screen switches between "Priced" and "Scope only". The scope-only version shows the work, the includes and the excludes with every dollar figure removed — for sending to a GC or a client to agree WHAT is being done before bid-day numbers are settled. It also drops the per-unit prices and the "this price is good through" line, since both refer to a price that is not on the page.

- **Bids can now charge sales tax — and it starts switched off.** In Settings → Sales tax you switch it on, say whether materials and labor are taxable, choose whether the rate applies to what you charge or what you paid, and add the areas you work in with their rates. Nothing is filled in for you.
- **HelixBid does not know your tax rates, and says so.** There is no built-in rate table and there deliberately never will be: rates change constantly by state, county, city and district, and a stale built-in rate would look exactly like a correct one on a bid you have already sent. Every rate is one you entered and confirmed. The Settings page says this in plain language before any control, and each area records where its rate came from and when you last checked it — editing the rate clears that tick, because a changed number has not been checked.
- **A bid takes its rate from the job address.** The most specific area wins — a city rule over a county rule over a state one — and the bid tells you which area matched and on what, so the figure can be traced rather than taken on trust. Rates stack, so "State 6.25% + Cook County 1.75% + City 1.25%" shows as its parts and as 9.25%.
- **If the rate cannot be worked out, the bid says so instead of charging nothing.** A silent $0 and a genuine exemption look identical on a bid and cost very differently, so an unmatched address reports "no rate" in red, and the proposal screen warns you before you send a document with no tax on it.
- **Every bid can override it.** Pick a different area, type a rate directly, or mark the bid tax exempt with a reason. Exempt beats a typed rate, which beats a chosen area, which beats the address match. An exempt proposal prints an explicit $0 tax line with the reason rather than just leaving tax off — a customer entitled to an exemption should be able to see they got it.
- **Tax is never folded into a total.** The bid shows Bid price, sales tax and Total due as three lines; the proposal shows subtotal, tax and total. A total with tax hidden inside it is one neither you nor your customer can check.
- **Fixed a bug this uncovered:** two settings lookups arriving at the same moment could both try to create the same missing settings row, and the loser's error stopped the bid pricing at all. It now recognises the collision and reads the row the winner created.

- **This is v6.0.** The trade axis now runs through the whole data model rather than just the assembly library, clients are a real record with their own screen, and the way into the app changed — so the number moved a major rather than a decimal point. (v5.98 was skipped; it was already used as a label for an unrelated data cleanup.)
- **The last of the old project system is gone from the app.** Six leftover screens that nothing linked to have been deleted, and the two that were still reachable by typing their address — the old project detail page and its Trash — no longer resolve. They belonged to the superseded projects system; their addresses now land on the Dashboard like any other retired link.

- **Starting a bid from a plan is now one click on the Dashboard.** Two cards sit at the top — "Upload a plan" and "Quick bid" — as the two ways a job actually arrives. Uploading takes the file first, creates the bid named after it, and drops you straight into the takeoff screen with the upload already running; you no longer have to create a bid and then go hunting for a Plans tab inside it. The old "New bid" button is still there for setting up an empty shell, just quieter.
- **The old splash page is gone.** Signing in used to land on a leftover marketing page — logo, tagline, four feature cards and a "Go to Projects" button — that predated the Dashboard and did not fit the screen properly. The Dashboard is now the landing page, and old links to it go there too.
- **Modifiers moved below Assemblies in the sidebar.** The Library now reads in the order you build things: parts, rates, the recipes, what tunes them, then bundles.
- **The Proposal screen now says where a client's name came from.** With a client attached and the proposal's own Client box empty, the box showed nothing while the document was addressed to someone — so it now shows the attached client's name and explains that typing there overrides it for this one document.

- **Clients now have a screen.** Workspace → Clients lists everyone you bid for — companies or individuals, with a contact, address, phone, email and private notes — and lets you add and edit them. Search covers all of it, so you can find a customer by the street they are on or a phone number, not just the company name.
- **Removing a client archives it, the same as everywhere else in the app.** It comes off the working list and can be restored at any time. There is deliberately no permanent delete: the bids attached to a client are what make the record worth keeping, and destroying it would quietly detach every one of them. The confirmation says how many bids are attached and that none of them are touched.
- **A bid can now be attached to a client.** The bid screen has a Client box: search the list, pick one, or type a name and add them on the spot without leaving the bid. Attaching one fills the client's details into the proposal.
- **A name typed on the bid still wins.** If you have written a client name on the Proposal screen, attaching a record does not overwrite it — the bid keeps what you typed, and the box says so plainly and names what it is superseding, so an attached client and a different name on the document is never a mystery. Bids with no client behave exactly as before.

- **A failed plan upload now has a Retry button.** It reuses the file you already picked, so there is no hunting through folders for it again and no reloading the page. Retrying is the same code path as the first attempt, so it behaves identically rather than being a slightly different second version. Failures that the file itself causes — too large, or not really a PDF — offer only Dismiss, because sending the same bytes again could never work and a button that cannot succeed is worse than no button.
- **Picking another file no longer wipes a failed one off the list.** New files are added to the list instead of replacing it, so the row explaining what went wrong stays until you dismiss it. Also fixed: dismissing one upload while another was still going could send the second one's progress to the wrong row.

- **Found why plan upload was failing completely: it is a storage setting, not a file size.** The browser uploads plans straight to storage, and storage was refusing the request before any of the file left the browser — so every upload failed, at every size, no matter how small. Nothing about the file was ever the problem. The fix is one configuration change on the storage bucket, written up in `references/deploying.md` § 9; it cannot be made from the codebase.
- **Uploads work again in the meantime, up to 25MB.** Plans now go by way of the HelixBid server when the direct route is blocked, which storage cannot refuse. That detour has a smaller size limit than the direct one, so larger sets still cannot be attached until the storage setting is corrected — and the app says exactly that, rather than pretending the file is too big and telling you to split it. As soon as the setting is fixed the app goes back to the direct route by itself.

- **The plan upload limit is now 500MB, up from 150MB.** A full commercial set that has been printed and re-scanned runs 200–600MB, and those were being turned away. Vector sets were never the problem; scans always were.
- **A failed upload now says what actually went wrong.** Every network-level failure used to show the same sentence — "The connection dropped during upload." — whether the connection dropped, the file never left the browser, the device was offline, or storage refused it outright. Four different problems, one wrong answer, and it sent people retrying on a better connection for something a better connection could not fix. Each now has its own message, says whether retrying is worth it, and keeps the technical detail on screen instead of in a toast that vanishes.
- **An upload that stops moving is now given up on rather than hanging forever.** There was no timeout of any kind before. It watches for silence rather than total time, so a genuinely long 500MB transfer on a site connection is never cancelled just for taking a while.
- **Very large plans warn before they are opened.** Anything over 150MB still attaches, but says up front that a set that size can be slow to open and may not render on a low-memory device — rather than implying everything under the limit opens comfortably.

- **Every part of the library now knows which trade it belongs to.** Materials and assemblies already did; labor rates, kits and the company settings do now. Nothing looks or behaves differently today, because electrical is still the only trade — the point is that adding plumbing or HVAC later means adding tagged records rather than rebuilding screens.
- **Shared things stayed shared.** Labor rates, company branding, pricing defaults and proposal terms are marked as applying to _every_ trade rather than to electrical. Tagging them "electrical" would have read as correct today and hidden a contractor's rates and licence details the day a second trade was switched on. Each of these can now also carry a trade-specific version — which matters most for the licence number, since a contractor licensed for two trades holds two separate numbers and printing the wrong one on a quote is a legal problem, not a cosmetic one.
- **There are now proper client records.** A client — company or individual, with address, phone and email — can be created once and attached to any bid, instead of the customer's name being retyped on every job. This is the foundation for looking up what you have quoted a repeat customer before, for working out sales tax from the job address, and for government bid paperwork later; none of those are built yet.
- **Assigning a client is optional and always will be.** Every existing bid has none, and a bid written in a hurry never needs one. A bid with no client behaves exactly as it did before, and a name typed directly onto a bid still wins over the linked record — so correcting a client's name for one proposal cannot quietly change every other bid pointing at that customer. Deleting or archiving a client never deletes the bids attached to it.

- **The backup now runs itself, nightly.** It exports to Cloudflare R2 every night at 2am UTC without anyone triggering it. It runs an hour and a half before the archive purge that permanently deletes expired bids, so each night's copy still contains whatever the purge is about to remove.
- **A failed nightly run is loud, in three places at once** — the platform records the failure and retries, the full summary goes to the server log, and a record of exactly what failed is written into the backup bucket beside the data. A partial backup counts as a failed one. If the credentials are ever missing from the deployed app, every run fails visibly rather than quietly doing nothing.
- **A retry will not repeat a backup that already worked.** If a night's backup succeeded and the platform retries anyway, it checks the bucket, sees the finished copy and stops. A run that failed halfway leaves no such record, so the retry does the work.

## [2026-08-13]

- **There is now an independent backup of everything.** One command exports every database table and every uploaded file — plan PDFs, company logos — to a Cloudflare R2 bucket that has nothing to do with Manus. Each run lands in its own timestamped folder alongside a manifest saying exactly what was copied and what, if anything, failed. Run it with `pnpm tsx scripts/backup.mts`; there is also an admin-only button-equivalent for taking one in a hurry.
- **The backup can now be proved rather than assumed.** A second command downloads the backup back out of Cloudflare, rebuilds it into a scratch database, and checks that what comes back matches what the backup said it saved. Uploading a file is not evidence it can be restored — this is. It refuses to run against the live database, so verifying can never damage the thing it is verifying.
- **It refuses to fail quietly.** The destination is checked before anything is read, so a wrong credential costs a second instead of an hour. A file that cannot be fetched is reported as a failure rather than skipped, and the run is marked failed even if everything else worked. The tables to export come from the database itself rather than a list in the code, so a table nobody remembered to declare still gets backed up.
- **HelixBid has a public landing page.** Anyone who visits the site without signing in now gets a proper page instead of a login box: one line saying what the app does, three steps explaining how it works, one real screenshot, and a form to ask for early access. Signing in is one click away in the header for people who already have an account.
- **It is deliberately quiet, and looks like the app rather than an advert.** Three sections, one button, one picture, and the same colours, spacing and controls the estimating screens already use. No gradients, no icon cards, no badges. A first draft with six sections and a differentiator grid was built and then cut back, because it looked nothing like the product it was selling.
- **The headline leads with what it does, not the name** — "Electrical estimating, built by an electrician" — because "HelixBid" tells a first-time visitor nothing, and someone who bounces off the first screen never finds out what was on offer.
- **The picture on it is the actual product.** A real screenshot of a real bid, with real quantities, real hours, and a total that breaks down into materials, labor, overhead and markup. No stock photos of people in hard hats.
- **The page is built so a second trade is content, not a rebuild.** Every trade-specific word, image and bit of vocabulary lives in one config file per trade; the layout and section order are shared. Adding plumbing means writing one file — no page code changes — and there's a test that renders the whole page with a made-up trade and fails if a single word of the electrical copy leaks through, or if the page ever grows a fourth section.
- **Early access signups are actually kept.** Submitted addresses go into the database with a timestamp, and there's a section at the top of Admin Settings listing them, with a checkbox to tick each one off as you contact them and a CSV export for the full list. Signing up twice is not an error — it says you're already on the list. A short privacy line sits directly under the field.
- **Search results and shared links now show something useful** — a page title and description written for a person scanning results, plus the product screenshot as the preview image when the link is pasted into a message or a post.
- **The landing page no longer downloads the whole estimating app.** The bid workspace, plan viewer and library screens are split into their own bundle that only loads once you sign in, cutting a visitor's first download roughly in half.
- **The Takeoff page can now read a plan sheet for you.** A new Plan reader panel looks at the sheet on screen, finds the device symbols you have linked in your legend, and offers to stamp each one where it saw it. It also writes a short plain-English summary of what the sheet asks your trade to do, and you can ask it questions about the drawing — "how many receptacles on the north wall?" — and get an answer without leaving the page.
- **It reads the symbols YOU taught it, not a generic idea of what an electrical symbol means.** Everything it proposes comes from your own legend links — the "click a symbol, link it to an assembly" system already there. A mark it cannot match to your legend is still offered, but as something to link rather than a match, because two architects draw the same device three different ways and only you know which is which on this job.
- **Nothing it finds reaches your bid until you tick it and press Place.** Reading a sheet writes nothing at all: every proposal sits in the panel, drawn on the plan as a dashed hollow ring so it can never be mistaken for a mark you placed. Confirmed proposals become ordinary stamps, through exactly the same path as clicking the stamp tool yourself.
- **Three levels of confidence, not two — and the third one is the point.** Confident matches arrive ticked and ready. Uncertain ones are flagged in amber and arrive UNticked, so accepting one is a decision rather than something you forgot to undo. And anything it genuinely could not make out — smudged, overlapped, cut off, a bad scan — is listed as "Needs your eyes" with no tick box at all: it tells you where to go and look and proposes nothing. It will not dress up a guess as a low-confidence match, because a wrong count that looks confident is worse than a gap you fill in yourself.
- **It counts; it never prices.** The reader extracts quantities and locations only. Every cost, labor hour and total still comes from the pricing engine that prices everything else, from the same frozen snapshots — so nothing on a bid is ever a number the AI worked out.
- **Correcting it teaches it — for your account, on that architect's drawings.** Tell it a mark is really something else and it reads it that way on the rest of that drawing set and on the next job from the same firm. Those corrections are yours alone, exactly like your materials, labor rates and assemblies; another contractor's reading of an ambiguous mark can never change what your plans say.
- **It reads one sheet at a time, as you open it, and only once.** A forty-sheet submission is not read up front — most of those sheets are schedules, details and other trades' work you will never take off. Paging back to a sheet shows what it found the first time at no further cost; re-reading is a button you press. There is a switch to turn the automatic reading off entirely.
- **If it cannot be reached, cannot read the sheet, or comes back with nonsense, it says so and changes nothing.** A bad scan produces a list of places to check and not a single proposal — including if you press "accept all".
- **Every assembly can now carry its own overhead hours** — the time a job takes that no material line explains: laying it out, ringing it out at the end, cleaning up, the trip. It sits under Labor in the assembly editor with a note saying what belongs in it, and the running total under the box shows the sum as you type ("1.25 h + 0.5 h = 1.75 h before modifiers").
- **It is set per assembly, in hours, not once for the whole company.** A receptacle swap and a panel change carry completely different amounts of setup, and neither has much to do with how many devices are in the recipe — so whoever knows that work sets it on that assembly. Every assembly, including every starter, ships at 0, so nothing on any existing bid moved.
- **It is not the productivity factor, and the two never touch.** Assembly overhead is a fixed number of hours added at the very start; the productivity factor is a company-wide percentage applied at the very end. The order is: material hours + overhead hours, then job-condition modifiers, then the productivity factor. That means a hard job stretches the setup time too, and a crew that beats book hours beats them on cleanup as well — but 20 minutes of trip time never quietly becomes a percentage of anything.
- **A finished bid can now be turned into a proposal you would actually hand a client.** Open any bid and click Proposal: your letterhead at the top, who it is for, what is included, what it costs, and signature lines at the bottom, on a real US-Letter page you can print or save as PDF straight from the browser.
- **The proposal shows a client a price, not your costs.** Materials, labor, overhead, profit and the productivity factor all go into the number, and none of them appear on the page as separate lines — a client sees "Total investment", the work included with quantities, and nothing that invites a line-by-line argument about your margin. On a bid with repeating units it can also show a per-room price, scaled so the rooms add up to the total rather than to the bare cost.
- **It quotes what was actually priced.** The document is built from the frozen snapshot each line was added with, so re-pricing a material next month does not silently reissue a quote you already sent.
- **Settings has a Branding section: logo, company name, license number, address, phone, email and website.** It is yours — nothing is filled in for you, and nothing is shared with another account. Anything you have not filled in is flagged in Settings AND printed on the document as a visible prompt like "[Add your license number]", never as blank space, so a half-finished letterhead cannot be posted by accident. The same prompt appears beside the preview, with a link straight to the settings.
- **Three finished layouts to choose between — Classic, Modern and Minimal — plus an accent colour and switches for which sections appear.** Layout and colour are picked from thumbnails beside the live preview, so you can see what each does. There is no template upload and no free-form editor, deliberately: every choice is a document already laid out to be presentable. Your company header and the price cannot be switched off; everything else can, and a section with nothing to show (no terms written, no repeating units) leaves itself out rather than printing an empty heading.
- **Standard terms and a "price good for N days" line are set once and reused on every proposal**, and each bid carries its own client name, job address and opening note.

## [2026-08-12]

- **Every screen now lists materials in the same order: category, then type, then size.** The order used to be a hand-copied list inside the Materials screen, with a note asking whoever came next to keep it in step with the database by hand — and Supplier Pricing did not sort by size at all. One catalog listed two ways means the answer to "where is the 20 amp breaker" depends on which screen you opened. There is one rule now, in one place, and a test that fails if the two ever drift apart again.
- **Conduit groups by type before size.** All the EMT together across every size and fitting, then all the PVC 40, then PVC 80, then rigid, then IMC. It used to sort by size alone, which interleaved five different products at every trade size — so pricing "all the PVC" meant picking it out of four other things, nine times over.
- **Panels and Breakers are separate shelves now.** They shared one, so reaching the 20 amp breakers meant scrolling past every panel size first. Disconnects, meter bases and fuses stay with Panels — a fuse goes in a fused disconnect, not in a load center.
- **Breakers are ordered the way you'd count them**: the half-size tandems first, then standard single-pole by amperage, then every 2-pole by amperage. A protected breaker sits with its own pole count rather than in a separate block, so "the 20 amp singles" are together whether they are plain, AFCI or GFCI.
- **Two-pole breakers are now named "2-Pole", the way a supply house writes them** — "20A 2-Pole breaker" rather than "20/2". You can still find them by typing "20/2", "double pole", "two pole" or "DP"; only the label changed, and the rows kept their identity, so nothing that already used one was disturbed.
- **Twelve breakers that were missing are now in the catalog.** Only the single-pole AFCI/GFCI combo existed before, which meant a bedroom circuit (AFCI alone), a bathroom (GFCI alone) and a spa or well pump (2-pole GFCI) had no row to take off. Added: single-pole AFCI and GFCI at 15 and 20A; 2-pole GFCI at 20, 30, 50 and 60A; 2-pole AFCI and 2-pole AFCI/GFCI combo at 20 and 30A.
- **The test price put on THHN during last night's import check is gone.** #1 THHN is back to unpriced and flagged, along with an older $0.85 left on #10 THHN from an earlier session. No shipped material carries a price — that was checked across the whole catalog, not just those two.
- **There is now one materials list in this app, not three.** Supplier Pricing was running on its own separate 1,103-item catalog that shared nothing with the Materials library — two lists, two answers to "what does #12 THHN cost", and a bid could be built from either. That catalog is deleted, along with two other CSV importers hiding in Settings that each wrote to their own store. Supplier Pricing now reads and writes the **same materials** as everything else: set a price there and every assembly using that material follows immediately, because it is the same row.
- **Supplier Pricing tells you how old every price is.** Each row shows the days since the price was last set, coloured green under 30 days, amber from 30 to 90, and red past 90 — with filter chips to jump straight to the ones that have gone off, and a warning when any have. A missing price was already flagged; the dangerous one is the real-looking number nobody has re-checked since copper moved, and that one used to look identical to a price you set this morning. The age tracks the **price specifically** — renaming a material or fixing its category will not make a nine-month-old price look fresh.
- **You can record which supply house a price came from**, per material, right next to the price.
- **Importing a price list now prices your real catalog.** Paste name-and-price rows, name the supplier, and matching materials are priced and stamped in one go. Names that match nothing are **reported back rather than created** — a supplier sheet is full of items you do not stock, and adding each one would rebuild the two-catalog problem an import at a time.
- **Four legacy screens went with it**: the old Assembly Builder, the Labor & Material page, the estimating workspaces behind Residential/Commercial/Civil/Industrial, and the Unit Count catalog picker. All four priced from the deleted catalog, all four were already replaced by Bids and the Library, and all four are now gone rather than quietly disagreeing with the screens that replaced them. Their old addresses land on the Dashboard.
- **You can now put away a starter material, assembly or kit you never use.** Modifiers has always allowed this; the other three refused, so a contractor who works only in resi was stuck scrolling past commercial switchgear forever. All four now behave the same way: hover the row, click Archive, confirm — and it is off your working list. Nothing is deleted. It sits in the **Archived** tab, restorable at any time with no time limit, and **Delete Forever** stays where it always was, available only from that tab. Starters you have already customized are unaffected; this only closes the gap for ones you never touched.
- **Repeating units are now templates, and the copies stay connected to them.** Build one hotel room the way you want it, generate forty numbered copies, and every copy remembers where it came from. Change the template later — add a circuit, drop a fixture, change a quantity — and the app **asks** whether to update the copies that are still following it. It never does that on its own: the dialog says exactly how many rooms it is about to rewrite, because "update 3 rooms" and "update 140 rooms" are different decisions.
- **Edit one copy directly and it quietly becomes its own room.** The moment you change a line inside Room 105 — or delete one — that room stops following the template, and later template-wide changes leave it alone. This is the same rule the material library already uses: editing something shared gives you your own copy rather than changing everyone's. Every unit says which it is, in words and with an icon rather than colour alone: **Template**, **Linked**, or **Edited**.
- **One generate can now cover more than one room type.** Ask for 35 Standard Rooms and 5 ADA Rooms in a single action and you get Room 101 through Room 140 — one unbroken run of numbers, because a hotel numbers rooms by where they are in the building, not by which spec built them. Each type is simply its own template, so pushing a change to ADA Room touches its five rooms and nothing else. A simple job never sees any of this: the panel opens as the single-unit form it has always been, and a second type appears only if you ask for one.
- **Removing all forty rooms at once is now one action, and it is undoable.** Every template offers to archive the copies still following it. They come off the bid and the total drops to match, the template itself stays, rooms you edited separately are kept, and an **Undo** sits in the confirmation for ten seconds afterwards. Archived rather than deleted, so the frozen costs that made every copy price identically are still there if you want them back.
- **The app now reports its version honestly, and from one place.** The sidebar said "v5.50" and the home page said "v5.46" — two different answers to the same question, both of them stale by dozens of releases, because each screen carried its own hand-typed copy. Both now read the same number and take it from a single source, so they cannot drift apart again.
- **Your company-wide pricing defaults are now editable, and every one of them says what it reaches.** Settings has a new **Bid pricing defaults** section for default overhead, default profit method and percentage, and the productivity factor. Each carries the same plain warning: _this is your company default — it affects every new bid going forward, and every existing bid still set to follow it._ That second half is the part people do not expect, because bids **follow** these settings rather than copying them, so changing one moves bids you have already built unless that bid was given its own setting. What a change can never do is alter what a finished bid was built **from** — the materials, hours and rates frozen onto each line when it was added stay exactly as they were, and there are now tests holding that line. Overriding a setting on one bid is unchanged and stays an ordinary local edit, with no company-wide warning attached to it.
- **The bid breakdown now shows the productivity factor doing its work.** When the factor is set, the Labor line spells out the arithmetic — _"20.4 h after modifiers, +12% productivity"_ — alongside the adjusted total, and says whether the factor came from your company default or from that bid. Without it the hours on a bid quietly disagree with the hours on your assemblies and the only way to find out why is to go hunting in Settings. It appears in Quick bid the same way. At 0% nothing extra is shown, because there is nothing to explain.
- **New: a productivity factor.** One number that adjusts all labour hours up or down company-wide, for how your crews actually perform against the hours written into your assemblies — positive if the work takes longer than the book says, negative if your crews beat it. It ships at 0%, does nothing until you set it, and is fully reversible: it is applied when a price is calculated and never rewrites the hours stored on your assemblies, so putting it back to 0% returns every number to exactly where it was. It is **not** another job-condition modifier and never gets added in with them — modifiers describe one job's conditions and add together, while this is applied afterwards as its own separate step, so you can always see which of the two moved your hours. You can override it on a single bid like any other setting.
- **The "where do I go?" helper now points at Claude**, the same AI the rest of the app already uses, on its cheapest tier — this is looking up which of a dozen screens you want, not heavy thinking. It had been configured for a different provider's model that the app has no account with, so it would have quietly never answered. When it cannot reach the AI it now records the reason _and which model it tried_ in the server log, so a misconfiguration is something you can look up rather than guess at; on screen it still just says it isn't sure, because a missed navigation hint is not worth an error message.
- **The sidebar is now grouped into three labelled sections instead of one flat list of eleven.** **Workspace** at the top is what you open daily — Dashboard, Quick bid, Bids. **Library** in the middle is what you build once and reuse — Materials, Labor Rates, Modifiers, Assemblies, Kits, and your supplier price list. **Settings** at the bottom is what you set up and forget. When the sidebar is collapsed to icons the sections are separated by thin rules, so the three purposes read as three things whether it is open or not. Two entries are gone from the sidebar: the old **Assembly Builder**, which was superseded by Library § Assemblies and sat confusingly next to it under an almost identical name, and **Trash**, which held deleted items from the old projects system and is replaced by the bid **Archive** now shown under Settings. Both pages still exist at their old addresses, so nothing you have bookmarked breaks. **Material Database** is now called **Supplier Pricing** — it is the price list you import from your supply house, and calling it "Material Database" put it one letter away from the Materials catalog, which is a different thing entirely.
- **A new account now starts somewhere built for a first day**, rather than on the full Dashboard with every screen showing at once. It asks for one thing — what an hour costs you — and then puts you straight into a Quick bid. Just one step, and you can skip it, but it says plainly what skipping costs. Labour is asked for first because it is the number that multiplies _every_ line of a bid: a few unpriced materials understate a job slightly, whereas a $0 labour rate leaves out the entire labour cost while the bid still looks finished.
- **Starter labour rates now ship at $0, flagged "Needs rate"**, the same way materials do. They used to carry plausible round figures, and a plausible number nobody chose is indistinguishable on screen from one you set. Rates you have already entered are untouched.
- **A getting-started checklist on the Dashboard**: set your labour rates, price your first material, build your first assembly, complete your first bid. It ticks from your actual data, never from having visited a screen — a checklist that congratulates you for opening a page is how someone ends up believing they are set up when they are not. Close it whenever you like and bring it back from the same spot; it retires itself once all four are done.
- **A "where do I go?" helper.** Type a plain question — "where do I edit labor rates" — and it points you at the screen, with a button to go there. It picks from a fixed list of real screens rather than making up a destination, so it can never send you somewhere that does not exist, and if it is unavailable it just says so and gets out of the way.
- **Materials are now listed by size, smallest to largest, within each category.** They used to be alphabetical, which is close to meaningless for this trade: it files 1" EMT between 1/2" and 1-1/4", and puts #1/0 wire next to #1 instead of above it. Wire is the awkward case and it is now handled explicitly — American Wire Gauge counts _backwards_ (18 is thinner than 14, which is thinner than 1), then flips at 1/0 and counts upwards again through 4/0 before turning into kcmil. Sorted as plain numbers you get 1, 2, 3, 4, 10, 14; sorted as text you get 4/0 sitting between 4 and 6. Both of those look sorted on screen, which is exactly why they are dangerous — you scan for the next size up and find the wrong row. The correct order is now written out as a table rather than calculated.
- **The Materials screen stays fast at 600+ items.** It only builds the rows you can actually see — about twenty at a time instead of all 619 — so scrolling, searching and filtering stay immediate as your catalog grows.
- **Track lighting and its heads are separate items now**, because a section of track does not light anything on its own and a takeoff listing only the rail has forgotten most of the cost. Track comes in 4', 6' and 8' sections. **Under-cabinet lighting is split the same way**: rigid bar fixtures at 12", 18", 24" and 36" sold per unit, and LED tape light sold by the foot, because pricing a cut-to-length reel as a single unit is wrong by an order of magnitude either way.
- **Cartridge fuses are their own item**, at every amperage of fused disconnect in the catalog — 30, 60, 100, 200, 400 and 600A. A fused switch ships empty and the fuses are a real cost that gets replaced later; folding them into the switch as an option is how they vanish from a takeoff.
- **Crimp lugs are now sized the way they are actually sold** — by the range of conductor they accept (14-10, 8-6, 4-2, 1-1/0, 2/0-4/0 AWG) rather than one row per gauge. The old per-gauge rows implied a precision that does not exist and left you hunting for a "#3 lug" that no supply house stocks.
- **The duplicate 6" wafer light is gone.** The 5"/6" wafer already fits both trim openings, so the two rows were the same product listed twice — a choice between a part and itself.
- **The starter catalog now has about 600 materials instead of 29.** Wire and cable down to the individual size (THHN solid and stranded, aluminium feeder, all nine NM-B sizes, nineteen MC sizes, UF-B, SER/SEU, bare copper ground); five conduit families — EMT, PVC 40, PVC 80, rigid and IMC — at every trade size from 1/2" to 4", each with its connector, coupling, 90 and LB; flex and liquidtight; strut by profile with its straps, nuts and all-thread; boxes in plastic and steel; the full breaker, panel, meter and disconnect range; lighting; grounding; life safety; fasteners; connectors and consumables; the residential equipment a job connects at the end; and placeholder rows for commercial gear. Low-voltage material — Cat6, coax, speaker and control wire, landscape lighting — is filed under its own trade rather than mixed into the electrical list. **Nothing already in your library was duplicated:** of the 29 materials that were there, 26 kept their names and their rows exactly as they were, and three were renamed in place to fit the families that have grown up around them (`1/2" PVC` is now `1/2" PVC Sch 40`, and the two EMT connectors read size-first like the 224 fittings beside them). Renaming in place rather than replacing matters because your assemblies, kits and takeoff counts point at those rows.
- **Every starter material now costs $0, and the app says so.** The old catalog carried plausible-looking estimate prices, which was the more dangerous choice it looks like the opposite of: a stale guess is indistinguishable on screen from a price you checked, so a bid could be built, sent and won on numbers nobody ever verified. An unpriced material now reads **Needs price** where the figure would be, and a **Needs pricing** filter on the Materials screen shows only those, with a count — so "my catalog is unpriced" becomes a list that visibly shrinks as you work through it rather than a vague worry. Prices you have already put on your own copies are untouched, and stay untouched on every restart. One honest consequence: a starter assembly's material cost is $0 until you price its parts, so its labour hours carry the whole figure.
- **You can record the brand or part number you actually buy.** The catalog is deliberately generic — "Wire nuts", not one manufacturer's part, because a shipped catalog naming one brand is wrong for everyone who buys another — so each material now has an optional note for the one you order.
- **Searching "romex" returns the cable again, not the staples that hold it.** Found while checking search quality against the bigger catalog: the word was recorded in two places at once, which was enough to lift an accessory above the product it serves. The same class of mistake as the old "recep" bug, and now covered by tests that check ranking across the whole catalog rather than item by item.
- **Strut channel nuts are now in the starter catalog**, and searching "spring nut" finds them — along with "unistrut", "kindorf", "superstrut" and the thread sizes. The item was not there at all before, so there was nothing for the alias to attach to.
- **A material you add by hand can now carry its own trade slang, and the app will suggest it.** Until now the add form only asked for a name, category, unit and cost — the trade-slang field existed in the database but nothing on screen ever filled it in, so a material you added yourself was findable by the common abbreviations everyone shares and invisible to the specific words people actually say for it. Every starter material has that slang; yours did not. There is now a field for it, plus a **Suggest** button that proposes the terms an electrician would type. Suggestions arrive switched off and you tick the ones that are right — nothing is added on your behalf, because a wrong alias quietly spoils every future search for that item. Anything the suggestion offers that names a _different_ material in your catalog is thrown away before you ever see it.
- **Trade slang can now be edited on a material you already have, not just set when you add one.** The field and its Suggest button now appear when you edit any row in the Materials library, so a term that was typed wrong, missed, or never added can be fixed — previously slang could only be set at the moment a material was created, which left a bad or missing term permanently wrong on a row you could otherwise change freely. This works on starter materials too: editing one gives you your own copy as it always has, and the copy keeps the words you chose. Clearing the field is treated as a real decision and stays cleared, rather than being quietly refilled from the starter list the next time the app restarts.
- **Layers.** Two colour-coded checklists in the takeoff panel: **System** (what a thing is — Devices, Lighting, Panels, plus conduit and cable runs) and **Location** (where it sits — Underground, Slab/Floor, Wall, Ceiling/Overhead, Exposed, Roof). They filter independently and combine, so "just the devices, and just the ones in the wall" is one setting rather than a choice between two. Turning a layer off hides it both on the drawing and in the counted-items list at the same time, and the panel says plainly when you are looking at part of a sheet — a filtered takeoff that looks complete is how a job gets quoted missing half its receptacles. Everything starts switched on, and anything new that appears arrives visible.
- **Where something sits is tagged on the item, not the assembly.** A receptacle is always a Device, on every job — but the same receptacle can be in a wall here and a ceiling there, so Location is set on the placed mark or the traced run. You can tag one at a time, or tag every stamp of one assembly on a sheet in a single action.
- **Legend capture, finished properly.** The browser pop-up that asked for a symbol's name is gone. Capturing now means dragging a box around the symbol on the drawing; the crop becomes a small picture stored beside the entry, and the naming happens in a proper on-screen form that behaves like the rest of the app. Legend entries are now recognisable at a glance rather than by name alone.
- **The stamp tool.** Pick an assembly once, then click every place it occurs on the plan — no re-selecting between clicks and no quantity to type. Each click drops a marker and the count climbs on its own. Markers are deliberately plain high-contrast rings rather than imitations of the drawing's own symbols, so you can see at a glance what you have already counted against what you have not. Click any marker to select it and remove it if you misclicked.
- **A live counted-items list.** Everything you have placed or traced on a sheet, in one panel: stamped assemblies with their quantities, and traced runs with their footage. Every entry has numbered chips — click one and the drawing rings that exact marker, so finding the third of twelve receptacles is a click rather than a hunt.
- **A legend that remembers.** Capture a symbol off the plan's legend and name it. The first time you click it, it asks once which assembly it matches; every click after that loads that assembly straight into the stamp tool. **Links are kept against your account, not the job**, so a symbol linked on one set of plans is already linked on the next one — which is the whole point. Choosing the assembly uses the same search as the Assembly Builder.
- Stamps get the same protection tracing already had: clicks are queued and saved in batches, mirrored in the browser as you go, and anything clicked but not yet saved is recovered and re-sent when you come back.
- **You can now trace conduit and cable runs on a plan and get real footage.** Pick "Trace conduit" or "Trace cable", click along the route, and the length updates as you move — finish with Enter or a double-click, undo a point with Backspace, back out entirely with Escape. Each finished run appears in the right-hand panel with its footage, and the bid's running totals sit underneath.
- **Conduit and wire are counted separately, and deliberately so.** A conduit run counts its pipe **once**, no matter how many circuits go down it. Every circuit you assign to that run gets its **own full length of wire** for each of its conductors — three circuits of three wires in a 100 ft pipe is 100 ft of conduit and 900 ft of wire, not 300 of either. Cable runs (Romex, MC) count as cable only, with no conduit line at all, because the cable is its own raceway. Getting this wrong in either direction costs real money, so the two numbers are never combined anywhere.
- **Nothing can be measured on a sheet without a scale.** If a sheet has no scale, or the drawing itself says "not to scale" and you have not overridden it by hand, the tracing tools simply are not offered and the drawing says why and where to fix it. A run traced before a scale was set is kept and stays unmeasured — it counts for nothing until you set one, rather than quietly contributing a wrong number. If you change a sheet's scale after tracing, affected runs say so rather than silently re-pricing.
- **Trace work is protected from the moment you start.** Progress saves to the server every few seconds while you are tracing and mirrors to the browser on every click, so a crash, a closed tab or a reload does not cost you a forty-click route. You are warned before leaving with unsaved progress, and an interrupted trace is offered back when you return to that sheet.
- **Deleting from your library no longer loses anything.** Removing a material, assembly or kit used to hide it with no way to get it back — it looked like a soft delete but behaved like a permanent one, because nothing in the app could show you what had been hidden. All three now work the way Modifiers already did: removing asks first, says plainly that the item is being **archived and not deleted**, and puts it in an **Archived** tab you can restore from. There is no time limit — unlike an archived bid, these are small records with nothing attached, so they keep indefinitely. Deleting something for good is a separate action available only from the Archived tab, with its own warning that it cannot be undone. Removing a modifier now asks first too, which it previously did not.
- **You can now see just your own library items.** Every library screen has an **All / Mine** switch with counts, so you can look at only what you have created or customised instead of hunting for your two edited prices among twenty-eight shipped ones. On the labelling audit that prompted this: all five screens were already correct — an unmodified shipped item says "Starter", a starter you have edited says "Your copy", and something you built from scratch says "Yours" — but the rule was written out five separate times, so it now lives in one place with tests pinning the direction.
- **Fixed a way to lose a bid by mis-clicking.** On the Bids list, the small trash-can button archived the bid the instant you clicked it — no question asked, no undo offered, and nothing on the page to say where it had gone. It never deleted anything permanently, so nothing was ever actually lost, but a stray click made a bid vanish from the list with no explanation. It now asks first, in the same words the Dashboard uses: the bid moves to the Archive and is permanently deleted in 30 days unless you restore it, and nothing about it changes meanwhile. The icon is an archive box rather than a trash can, because it does not delete. Confirming shows an **Undo** for the mistake that gets through anyway, and an **Archive** button now sits at the top of the Bids list — as it already did on the Dashboard — so archived work is one click from anywhere you can archive from. Checked at the same time: there is no way anywhere in the app to permanently delete a bid in one step. Permanent deletion exists only inside the Archive, only for something already archived, and the server refuses it otherwise.
- **Plan PDFs can now be up to 150MB, and you can watch them upload.** The old limit was 30MB, which turned away most scanned commercial sets. Raising the number alone would not have worked: files were being sent through the app server, and the hosting platform caps any single request at 32MB, so in practice nothing much over ~24MB ever made it regardless of what the app said. Plans now upload straight to storage from your browser, which removes that ceiling entirely. Because the browser is doing the sending, you get a **real progress bar** — a percentage and "82.1MB of 150MB", not a spinner that leaves you guessing whether a ten-minute upload has frozen — and a Cancel button. Uploading several sets sends them one at a time, so the first is usable while the rest arrive. If a file is too big or is not really a PDF despite its name, it says so in plain words and the message stays on screen until you dismiss it, rather than vanishing with a toast.
- **The plans screen now works like a real takeoff workspace.** It is a split view: the sheet index down the left, the drawing in the middle, and a reserved panel on the right that the counted-items list will fill in a later phase (it says so, rather than sitting there blank). You can drag the divider to give either side more room. **Sheets have real names** — where the PDF carries bookmarks, which most architectural sets do, the index shows "E1 - Power Plan" instead of "page 1"; where it does not, every sheet still gets a "Sheet 3" label you can rename, so you are never left clicking through blank page numbers to find the panel schedule. A name you type sticks, and reopening the drawings never overwrites it. Clicking any sheet jumps straight to it.
- **Each sheet carries its own drawing scale.** The app reads the scale off the sheet where it is stated plainly, shows it in the toolbar marked **Detected** so you always know where it came from, and counts how many sheets are scaled at the top of the index. Where a sheet states two scales — a plan and a blown-up detail, which is common — it deliberately does **not** pick one, because a wrong scale would make every measurement wrong by a constant amount with nothing looking broken; instead it offers what it found as a one-click suggestion. Setting the scale by hand is always available whether or not anything was detected, either by typing it (`1/4" = 1'-0"`, `1" = 20'` and `1:100` all read correctly) or picking from the common list. Scales are stored per sheet, so a site plan at 1" = 40' and a detail at 3/4" = 1'-0" can sit in the same PDF.
- **Bids can carry plan PDFs now** — the first piece of the rebuilt takeoff screen. Open a bid and there is a **Plans** button beside the due date; from there you can drop as many PDFs onto the job as it has sheets, or click to pick them, and read through any of them without leaving the app. The sheet list down the side shows every plan with its page count and size, and paging works with the arrow keys, Page Up/Down, and Home/End as well as the on-screen arrows, with the page number shown as "3 / 18" so you can see how big the set is before you start. Measuring and takeoff tools are not here yet — this phase is about getting the drawings onto the bid and being able to read them.
- **Bids can be archived, and the archive really does empty itself.** Archiving takes a bid off the dashboard without deleting it — useful for a job that is finished or one you have stopped chasing — and it works at any status, so you never have to mislabel a job as Lost just to get it out of the way. Archived bids sit in a new **Archive** screen with the number of days left and the actual date they will be deleted, soonest first, and a Restore button that puts everything back including its plans. After 30 days they are deleted permanently, for real: a nightly job now does the deleting. The old Trash screen counted down to a deletion that never happened, which meant the number on it was decoration; this one is enforced.
- Fixed the plan renderer, which had stopped working after a library upgrade and was quietly falling back to drawing PDFs on the main thread — the thing that makes the app freeze for several seconds on a dense drawing.
- Pressing Enter in the labor-rate pencil now saves the rate **and** closes the little panel, instead of saving it and leaving the panel sitting open for you to click away from. Escape does the same — throws the edit away and closes. Editing a rate is one keystroke to finish now, not a keystroke and then a click somewhere else. Typing a column of quantities on a bid is unchanged: Enter there still saves and stays put, ready for the next figure.

## [2026-08-11]

- You can now change a labor rate straight from the Assembly Builder — a small pencil next to the role opens the rate right there, no trip to the Labor Rates screen. It says plainly that you are editing the one shared rate used across your whole library, not a rate for that assembly alone, and tells you how many assemblies will reprice. Bids you have already built keep the rates they were priced at.
- Fixed a serious bug this uncovered: editing one of the built-in labor roles quietly dropped the labor cost to **$0** on every assembly using it. Editing a built-in role makes your own copy of it, and assemblies were still pointing at the original — which then went into hiding, so nothing was found and the labor priced at nothing. Assemblies now follow the copy correctly. This affected assembly and kit costs; bids that had already frozen their costs were never at risk.
- Added the **Dashboard**, which replaces the old Projects page as the app's home view. It shows every bid in four columns — Draft, Active, Won, Lost — with each bid's name, value, deadline and line count on the card, so you can size up where everything stands without opening anything. Draft and Active sort by deadline with the soonest first, and bids with no deadline set drop to the bottom of those columns rather than crowding out the ones that have a real date. Won and Lost sort by whatever you touched most recently. Overdue and due-today deadlines are colour-coded, and the header shows what you have out for bid in total. **Bids now have a due date** — optional, since not every job has a firm deadline — which you set from the bid itself. Clicking any card opens that bid.
- Five changes that make building assemblies faster. **Small parts come with a sensible count** — add wire nuts and you get 3, not 1, because nobody fits a single one; same for straps and connectors, and every one is still just a starting number you can change. **Any assembly can be duplicated**, starter or your own, giving you a separate copy to modify — unlike editing a starter, which quietly makes it _your_ version of that starter; a duplicate is its own thing and editing it never touches the original. **The materials you use most appear first** in the Assembly Builder before you type anything, so the same dozen parts are one click away. **The whole add-a-material flow works without the mouse**: type, arrow to pick, Enter to add, the quantity is waiting and selected, Enter again and you're back in the search box for the next one. **And there are now Kits** — named bundles like "Bedroom package" (4 receptacles, a switch, a light) that drop a whole room onto a bid at once. Three starter kits ship with it. A kit's items land as ordinary line items, each with costs frozen at that moment and each editable afterwards, so one room being slightly different is just an edit to that line. Quantities inside a kit are yours to set from experience — nothing works them out from room size or spacing rules.
- Added **Quick bid** — a fast way to price a job you already know the counts for, with no plan or takeoff involved. Name the bid, then it's just type, arrow, Enter: search an assembly, press Enter, it's on the bid. The search box keeps focus so you never touch the mouse, the quantity sticks between entries (six of these, three of those), and counting the same assembly again adds to its existing line instead of piling up duplicate rows. The bid price updates as you go. Repeating units — duplicate rooms and the like — use the same generator the full Bids screen has. Everything underneath is the bid structure already built, so a quick bid and a full bid are the same thing, priced the same way.
- Fixed garbled characters in a source file's comments, introduced by an earlier tooling mistake. No effect on how the app behaves.
- Numbers are now quicker and safer to edit everywhere in the app. Clicking or tabbing into any number — a rate, a percentage, a quantity, an hour count — selects what's there, so typing replaces it instead of you having to clear the box first. Fields that save on their own now also save when you press Enter, and pressing Escape abandons whatever you typed and puts the last saved number back without saving anything. When a number does save, the field flashes green for a second so you can see it landed rather than having to guess — this app is about getting numbers right, and you should never have to wonder whether one took.
- Added **Bids** — the layer where assemblies become an actual job with a price. Create a bid, give it a status (Draft, Active, Won or Lost) and the trades it covers, then search your assembly library and add what the job needs with quantities. The moment you add an assembly its costs are frozen onto the bid, so editing a material price or an assembly next week cannot quietly change a bid you already sent out. The running total shows materials, labor hours, direct cost, overhead and profit, ending in the bid price. Overhead and profit come from a company-wide setting that each bid can override on its own; the panel always says which one it is using. For repetitive work — hotel rooms, spec homes — build one unit's worth of line items, label it (e.g. "Room 101"), and generate as many numbered copies as you need in one go. Every copy prices identically to the one it came from, each stays independently editable, and the total keeps up automatically.
- Fixed a bug found while testing the new Bids screen: the "copy a unit" picker could come up blank and refuse to generate anything, because it read the list of units before that list had loaded.
- Added the **Assembly Builder** — the screen where Materials, Labor Rates and Modifiers come together into a reusable recipe like "Duplex receptacle standard". Search and add materials with quantities, pick which role does the work and how many hours it takes, switch on the job conditions that apply, and watch the cost update as you type. The preview shows materials, labor and direct cost, and will add overhead and profit once you choose a profit method — it never picks one for you, because markup and target margin give different prices. Eight starter recipes ship with it, each editable: changing one gives you your own copy and leaves the original for a fresh start later. New assemblies open with a suggested labor time based on what you're building (a receptacle suggests 0.75 hr, a panel 8 hr) rather than a blank or a zero, and the screen says plainly that the suggestion is a starting point rather than a verified figure.
- Fixed a bug in the new Assembly Builder before it shipped: saving a change to one field — say the name — would silently wipe the assembly's material list and reset which role does the work. Caught by the new tests.
- Added two new Library screens. **Labor Rates** lists what each role costs you per hour — Apprentice, Journeyman, Foreman/Master Electrician, Supervisor and Project Manager to start — and roles are your own, so you can rename them or add any you like. A role can be paid hourly or by salary; for a salary you enter the yearly figure and how many hours a year that actually covers, and the app works out the hourly cost from those two numbers (the starter Project Manager comes out at $28.85/hr from $60,000 over 2,080 hours). Both numbers stay editable, because most shops bill fewer hours than a full payroll year and billing at 2,080 quietly under-recovers what that person costs. **Modifiers** lists job conditions that make work take longer — working at height, occupied buildings, overtime, night shift and weekends — as percentages you can tune. Percentages add rather than multiply, and the screen shows the combined effect on a 10-hour task so you can see it. Removing a modifier moves it to an Archived tab rather than deleting it, where you can put it back; deleting for good is only possible from there and asks first. All starter figures are placeholders to get you going, not researched market rates.
- Fixed a bug that could quietly duplicate the built-in starter lists. If two copies of the app started at the same moment, each could decide the starter items were missing and add its own set, leaving every item listed twice. Now only one can do the setup at a time, and any duplicates already created are cleaned up automatically on the next start.
- Materials can now be found by what electricians actually call them. Every starter material carries trade slang alongside its catalog name, so "1900" finds the 4" square box, "gem box" finds the single-gang, "romex" finds NM-B cable, "plug" finds the duplex receptacle, and "load center" or "breaker box" finds the main panel. Twelve common terms that previously returned nothing now work. Also fixed two ways search could return the wrong item: a two-letter manufacturer code was matching in the middle of unrelated words (searching "breaker" returned a single-gang box, because "ch" is inside "switch"), and searching a category name was pulling in everything filed under it.
- Fixed search ranking so the word you typed wins. Searching "recep" was putting "Wall plate" at the top, above "Duplex receptacle" — the search knows that outlet covers and wall plates are related, and that association was outranking the item you were actually looking for. Search still finds things by trade slang ("romex" finds NM-B cable), but those now sort below anything that genuinely matches what you typed. This affects every search box in the app, not just Materials.
- Materials now have a category, and the Materials screen is shelved by it — Wire & Cable, Conduit, Conduit Fittings, Boxes, Receptacles, Switches, Wall Plates & Misc, Panels & Breakers, Lighting Hardware — so you scroll to the section you want instead of hunting one long alphabetical list. All 28 starter materials were sorted into the right section automatically, including copies you had already edited. Searching still looks across every category at once and lists the best matches first, with each result showing which section it came from. New materials you add can be filed on any shelf, or left unfiled.
- Planned out three ways to speed up counting items on a plan: a stamp tool that lets you pick an assembly once and click to drop it as many times as needed, AI that can drop those same pins for you automatically, and the ability to click a symbol in the legend to load its assembly instead of searching by name. Planning only — none of it is built yet.
- Decided how labor hours will work: they come from your own field experience with the NECA manual as background reading, and the existing Modifiers (height, outdoor, retrofit and so on) already cover what NECA handles with its Normal/Difficult/Very Difficult columns — so the app will not get a separate difficulty setting that asks the same question twice.
- The Materials library now has a real screen, reachable from the sidebar. It lists the 28 starter materials with their prices, lets you search by trade slang ("recep" finds receptacles), add your own, and edit any of them inline. Editing a starter material quietly hands you your own copy, marked "Your copy", with an undo button that puts the original back.
- Fixed `pnpm dev` on Windows — it was failing immediately with a "NODE_ENV is not recognized" error, so the app could not be started at all.
- Connected the new Materials library to the app. Editing one of the shipped starter materials now quietly gives you your own copy to change, leaving the original intact for everyone else, and you can undo back to the original at any time. Duplicate material names are refused, and prices are checked before saving. Still no screen — that's the last piece.
- Started building the new Materials library. This is the behind-the-scenes half: the app now ships with the 28 starter materials pre-loaded, and there's a working system for a contractor to take one of those shipped materials, make their own edited copy of it, and later undo their edits back to the original. Materials that are in use get hidden rather than deleted, so nothing an assembly points at can disappear. No screen for any of this yet — that comes next.
- Wrote out the parts list for all 27 first-tier starter assemblies — what materials go into a receptacle, a 3-way switch, an EV charger circuit, and so on. Labor hours are deliberately left blank; those come from the NECA manual. Also flagged 36 materials the assemblies need that aren't in the starter materials list yet, so they can be added with real supplier pricing.

## [2026-08-10]

- Fixed a real access-control security gap on project line items, assemblies, and bid summary settings
- Removed ~2,600 lines of dead code (unused Civil/Commercial/Residential calculator screens)
- Set up local development database, fixed a hidden bug in the test setup along the way
- Built and tested the Foundation: Materials, Labor Rates, Modifiers, and Assemblies data model, plus the pricing engine (44 tests passing), including a fix for a subtle money-rounding bug
- Documented the full estimating system plan (ASSEMBLIES_PLAN.md) and a starter content library (STARTER_LIBRARY.md)
